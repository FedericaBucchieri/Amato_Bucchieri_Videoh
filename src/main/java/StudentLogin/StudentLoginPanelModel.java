package StudentLogin;

public class StudentLoginPanelModel {
    // the Component controller
    private StudentLoginPanel controller;

    /**
     * This constructor creates an instance of StudentLoginPanelModel.
     * @param controller the MainLoginPanel instance that represents the controller of the component
     */
    public StudentLoginPanelModel (StudentLoginPanel controller){
        this.controller = controller;
    }
}
